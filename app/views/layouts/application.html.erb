<!DOCTYPE html>
<html>
  <head>
    <title>Recipe App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <header>
      <%= render "layouts/navbar" %>
    </header>
    <main>
      <div class="flex justify-center item-center">
        <div class="container mx-auto absolute top-0 mt-32 flex justify-center items-center">
          <div class="w-1/3 w-full">
            <% if notice %>
              <p class="notice py-3 px-6 bg-orange-200 rounded-md"><%= notice %></p>
            <% elsif alert %>
              <p class="alert py-3 px-6 bg-orange-200 rounded-md"><%= alert %></p>
            <% end %>
          </div>
        </div>
      </div>
      <%= yield %>
    </main>
    <footer>
      <%= render "layouts/footer" %>
    </footer>
  </body>
</html>
